import {describe,it,expect} from 'vitest';import {TrafficSimulation,type TrafficData} from './traffic';import type {RailNetwork} from './rail-network';
const network={nodes:[{id:'a',position:[0,0,0],type:'connection',osmNodeId:1,tags:{}},{id:'b',position:[1000,0,0],type:'connection',osmNodeId:2,tags:{}}],edges:[{id:'e',osmWayId:1,from:'a',to:'b',points:[[0,0,0],[1000,0,0]],lengthM:1000,maxSpeedKmh:80,electrified:true,service:'main',source:'OSM'}],switches:[],platformAssociations:[],playerRouteEdges:['e'],stats:{nodes:2,edges:1,switches:0,totalTrackKm:1,connectedComponents:1,playerRouteEdges:1}} as RailNetwork;const data={services:[{id:'t',tripId:'real',publicName:'1',headsign:'B',direction:'Rapperswil → Uznach',stops:[{name:'A',platform:'',arrival:'14:00:00',departure:'14:00:10',sequence:1},{name:'B',platform:'',arrival:'14:01:10',departure:'14:01:20',sequence:2}],path:[{edgeId:'e',reverse:false}],timetableClassification:'REAL',pathClassification:'DERIVED',motionClassification:'SIMULATED'}]} as unknown as TrafficData;
describe('timetable traffic',()=>{it('dwells, accelerates and occupies its rear edge',()=>{const sim=new TrafficSimulation(data,network,{A:0,B:1000},1000);expect(sim.update(14*3600+5)[0].dwelling).toBe(true);const moving=sim.update(14*3600+40)[0];expect(moving.active).toBe(true);expect(moving.speedMps).toBeGreaterThan(0);expect(moving.occupiedEdges).toEqual(['e'])});it('reset is deterministic',()=>{const sim=new TrafficSimulation(data,network,{A:0,B:1000},1000),a=sim.update(14*3600+40)[0].position.x;sim.reset();expect(sim.update(14*3600+40)[0].position.x).toBe(a)})});
